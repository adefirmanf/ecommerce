{
  "name": "ecommerce",
  "version": "1.0.0",
  "description": "Nuxt.js project",
  "author": "adefirmanf <syskeys32@gmail.com>",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "build:gh": "BUILD_ENV=gh nuxt build",
    "build:prod": "BUILD_ENV=production gh nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:gh": "BUILD_ENV=gh nuxt generate",
    "generate:prod": "BUILD_ENV=production nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "dotenv": "^8.2.0",
    "nuxt": "^2.0.0",
    "vue-lazyload": "^1.3.3"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^4.19.1",
    "eslint-friendly-formatter": "^4.0.1",
    "eslint-loader": "^2.1.1",
    "eslint-plugin-vue": "^4.0.0"
  }
}